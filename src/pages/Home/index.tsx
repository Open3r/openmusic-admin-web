import {Container, HomeText} from "./style";

const Home = () => {
    return (
        <Container>
            <HomeText>OpenMusic Admin</HomeText>
        </Container>
    );
};

export default Home;
